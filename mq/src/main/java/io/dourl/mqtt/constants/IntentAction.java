package io.dourl.mqtt.constants;


import io.dourl.mqtt.BuildConfig;

/**
 * 预先定义的各种intent action
 */
public class IntentAction {

    public static final String ACTION_WAKE_MQTT = BuildConfig.LIBRARY_PACKAGE_NAME + "." + "ACTION_WAKE_MQTT";

    //public static final String ACTION_PUSH_DISPATCH = BuildConfig.APPLICATION_ID + "." + "ACTION_PUSH_DISPATCH";

}